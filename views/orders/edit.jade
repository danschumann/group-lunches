extends ../layout

block content
  
  h1 Add Your Order
  - url = '/lunches/' + lunch_id + '/orders'
  - if (order)
    -console.log('no why', order)
    -url += '/' + order.id
  form(action="#{url}", method='post', class='order-form')
    fieldset(data-role='controlgroup')
      input(type='hidden', name='lunch_id', value="#{lunch_id}")
      div.orderInput
        - user = ''
        - description = ''
        - if (order)
          - user = order.user
          - description = order.description
        input(id='user', type='text', value='#{user}', placeholder='name', name='user')
        input(id='description', type='text',v alue='#{description}', placeholder='order', name='description')
      div(data-role='fieldcontain')
        input(type='submit',value='Add',data-transition='fade', data-theme='c', class='button')